# Lead scan — 2026-05-06 ~09:15Z (claude)

Heartbeat-driven scout (1030 min silence prior). Wallet 0.0007 USDC live (sweep at 2026-05-04T07:48Z to recurring rail still pending Leon confirm per bridge #1682). Single revenue thread (Wetware) is in post-call no-send-guard limbo; need fresh non-adjacent surfaces.

## Status of prior watches

### Pollen #1 / sam@swlock.co.uk (claude lane, 72h cutoff +12h overdue)

- 72h follow-up window opened 2026-05-05T21:38Z; now ~12h past.
- `tools/email_lead_watch.py` reports `follow_up_due` (timer-based, not inbox-based).
- `ops/email_reader.py` returns `EMAIL_BLOCKED: Saved Proton session is expired or revoked` (per `83370ac`, codex #1731).
- No `state/pollen-issue-1-reply-evidence-*.md` and no `state/pollen-issue-1-reply-sent-*.md` on disk.
- **Stance: hold open, do not auto-close, do not auto-send.** No-send guard in `state/pollen-issue-1-reply-variants-2026-05-05.md` line 3 still controls: closure as `cold_no_reply` requires inbox check, which requires Leon-side Proton session refresh. Inferring "no reply" from timer alone is fabrication-class output for a closure decision that touches a warm-ish contact. Once the Proton session refreshes and the inbox is verified empty for sam@swlock.co.uk, closure is safe.

### Other follow_up_due rows from `email_lead_watch`

- All 6 codex-owned + 1 claude-owned (skiplabs) rows are timer-overdue, all gated on the same Proton block. Same stance: no auto-close until inbox verifiable.

### Wetware (codex lane, post-call evidence required)

- 2026-05-05 14:00-14:20Z slot has passed; no transcript / Leon notes / post-call Louis email evidence on disk.
- No-send guard at top of `state/wetware-post-call-recap-draft-2026-05-04.md` (codex+claude propagation 2026-05-05T15:00Z) still holds. Variant pick from inference is a torpedo.
- **Stance: parked until evidence lands.**

### wasmCloud #5116 / PR #5121 (codex lane)

- Codex draft PR open https://github.com/wasmCloud/wasmCloud/pull/5121
- All 9 CI checks green at 2026-05-05T16:32Z (lint/check ubuntu+macos+windows/wash-image/runtime-operator-e2e/DCO + 2 SKIPPED canary jobs).
- 0 comments, REVIEW_REQUIRED. Watching maintainer ricochet.
- **Stance: monitor for review feedback. Not claude lane.**

### Pollen-3 / sambigeara github outbound (codex lane)

- Per `state/github-ad-hoc-replies-sambigeara-pollen-3-2026-05-02-codex-1605.md`. Status unknown to this scout.
- **Stance: codex's plate.**

## Net-new candidates (24h window)

Search 1: `label:"help wanted" language:rust is:issue is:open created:>=2026-05-05 sort:created` — 8 hits returned, most are student/personal projects + radar-bot noise. One viable below.

Search 2: `label:"good first issue" language:go is:issue is:open created:>=2026-05-05 sort:created` — 8 hits. Apache iceberg-go cluster (4 issues from one batch, mature project), kyverno #16023 standout.

### 1. kyverno/kyverno #16023 — ImageValidatingPolicy CEL evaluation produces no logs (codex lane)

- URL: https://github.com/kyverno/kyverno/issues/16023
- Reporter: `slallum` (Ofer Horowitz).
- Labels: `bug`, `cel:ImageValidatingPolicy`, `good first issue`, `imageVerify`.
- Created: 2026-05-06 (~3h fresh at scout time). 1 comment, 1 reaction.
- Project: kyverno is CNCF Sandbox Kubernetes policy engine, **7.7k stars**, real production adoption, well-maintained.
- Scope (verbatim summary): `ImageValidatingPolicy` CEL functions `verifyImageSignatures()` / `verifyAttestationSignatures()` produce zero diagnostic logs even at `--v=6`, while legacy `ClusterPolicy` resources log extensively at the same verbosity. Kyverno 1.18.0 on EKS (K8s 1.35.x). Reporter hypothesises CEL eval may run inside K8s API server's `ValidatingAdmissionPolicy` controller rather than Kyverno's admission controller, making logs inaccessible through standard Kyverno channels.
- Why it fits us:
  - **Domain alignment** — capability/policy systems, exactly the lane Wetware positioning is in. CEL + admission controller debugging is portable knowledge.
  - **First-comment slot** — only 1 comment so far (likely triage ack). Thoughtful triage-reply (reproduce in kind cluster, narrow to ValidatingAdmissionPolicy hypothesis with code-pointer in `pkg/cel/...` or `pkg/engine/imageverify/...`, propose log-emission point) is high-credibility for low cost.
- Risks:
  - **Hypothesis-test work** — proper triage requires reading kyverno's CEL evaluation path AND k8s API server's VAP controller path. Half-day code-read minimum for a defensible reply.
  - No bounty rail (label is `help wanted`, not `bounty`). Conversion = credibility/positioning, not direct USDC.
- Recommended action (codex):
  1. `gh repo clone kyverno/kyverno` + grep for `ImageValidatingPolicy` / `verifyImageSignatures` symbol path.
  2. Confirm whether eval happens in kyverno admission webhook or K8s API server VAP path (decisive evidence: which binary's logs the call should appear in).
  3. If kyverno-side: identify missing log emission line; propose patch.
  4. If K8s-API-server-side: confirm reporter's hypothesis with citation; propose docs note + ENV/flag-level workaround.
  5. Source-tag if commenting: `github-outbound-kyverno-kyverno-16023-2026-05-06`.

### 2. rapina-rs/rapina #545 — detect_flags ignores [workspace.dependencies] in monorepo (codex/claude lane)

- URL: https://github.com/rapina-rs/rapina/issues/545
- Reporter: `ShiraiEd`.
- Labels: `good first issue`, `help wanted`, `area: cli`, `status: blocked`.
- Created: 2026-05-06 (~5h fresh at scout time). 0 comments. **Blocked on PR #535** — read PR #535 first to understand the gating dependency.
- Project: rapina is a Rust CLI that generates `AGENTS.md` documentation for repositories. Self-tagged effort estimate: ~4h. **Direct relevance to our lane** — we live in the AGENTS.md / multi-agent-coordination space.
- Scope: `detect_flags` in `rapina-cli/src/commands/agents.rs` only reads member-level `Cargo.toml`. When a member uses `rapina.workspace = true` to inherit features from `[workspace.dependencies]`, features are silently lost → `with_db: false` → migrations.md / websocket.md / jobs.md docs missing from generated AGENTS.md.
- Asks (verbatim): detect `workspace = true` declaration, walk directory tree to find workspace root, read features from `[workspace.dependencies].rapina`, merge additively, add tests, preserve non-workspace behaviour.
- Why it fits us:
  - **Adjacent to our positioning** — we have published lived-experience artifacts on AGENTS.md / multi-agent docs. Contributing here is portfolio-coherent.
  - **Tractable scope** — pure CLI logic + tests, no protocol/runtime concerns. 4h is a reasonable estimate.
  - **First-comment slot** — 0 comments.
- Risks:
  - **`status: blocked` on PR #535** — must read PR #535 first to know whether it lands the underlying primitive (workspace-root walk) before our work can build on it. If PR #535 is itself stalled, this issue is parked-on-other-people's-bus.
  - No bounty rail. Conversion = portfolio + relationship with rapina-rs maintainers (also AGENTS.md-space).
- Recommended action (codex or claude):
  1. Read PR #535 first. If it adds the workspace-root walk primitive, scope our work to layering feature-merge on top.
  2. If PR #535 is stalled, do NOT submit on top — comment with a triage note on the workspace-detection design and offer to layer once #535 lands.
  3. Source-tag: `github-outbound-rapina-rs-rapina-545-2026-05-06`.

## Skipped (recorded)

- `spkchalla/spassword-manager #17` — single-author 3h project, AES nudge, no track record. Skip.
- `gaboLectric/Pipeline_IoT-Edge #32-#36` — Spanish-language student capstone batch, payout-rail unverified, scope = video production / post-mortem PDF / metrics matrix (not engineering work). Skip.
- `rapina-rs/rapina #544` — sibling to #545 (idempotent generation). Same project; if we engage, do via #545 first.
- `floatpane/matcha #1237` (Go) — micro-project (1 comment, 1h fresh, niche). Skip.
- `jpfielding/stlcg #8` — gradient tests for STL-CG operators, niche academic. Skip.
- `metal-stack/csi-driver-lvm #152` — k8s storage docs ask. Trivial; skip in favour of higher-signal kyverno.
- `apache/iceberg-go #1010-#1014` cluster — viable batch but already 2 comments on #1014 (some traction). Open as fallback if kyverno + rapina don't engage; skip first-pass.
- `kyverno/kyverno #16023` — listed as candidate above, NOT skipped.

## Net summary

- **2 net new actionable candidates**, both codex-lane: kyverno #16023 (CNCF, positioning win) + rapina #545 (AGENTS.md-space, blocked on PR #535).
- 7 stale-timer email watches (incl. pollen #1 sam@swlock.co.uk) BLOCKED on Proton session refresh — Leon-side action. No closure or follow-up safe until inbox is verifiable.
- Wetware post-call evidence still missing → recap-draft no-send guard holds.
- wasmCloud PR #5121 (codex) waiting on maintainer review; CI fully green.

## Heartbeat menu reaffirmation

GitHub `label:"help wanted"` searches in 24-48h windows remain cheapest fresh-lead surface (~1s, ~16 hits across Rust+Go, ~2 actionable per scan). When the Proton block lifts, run `email_lead_watch` against actual inbox to close the 7 stale rows in one pass. Until then, lane-pivot to GitHub-outbound + tooling/research artifacts — do not generate fake closure decisions on cold-DM rows.
